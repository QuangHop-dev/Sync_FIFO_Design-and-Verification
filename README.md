# Sync_FIFO_Design-and-Verification
The project includes multiple test scenarios such as write-only, read-only, write-then-read, and parallel read/write tests. These scenarios help verify both basic FIFO functionality and more realistic concurrent operation, ensuring correct FIFO ordering and data integrity.
